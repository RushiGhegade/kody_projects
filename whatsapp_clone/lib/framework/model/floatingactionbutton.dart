
import 'package:flutter/cupertino.dart';

class FloatingInfo{
  final bool isShowIcon;
  final bool isShowIcon1;
  final Icon icon;
  final Widget? widget;

  const FloatingInfo({required this.isShowIcon,required this.icon,this.widget,required this.isShowIcon1});
}